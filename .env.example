# Example environment variables for mixlrrep
PORT=3000
# Set to the origin your web client will be served from, e.g. https://yourdomain.com
CORS_ORIGIN=*
# Add other vars as needed (DB connection strings, secrets)
